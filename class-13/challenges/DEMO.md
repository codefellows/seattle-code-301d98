# Code Challenge Demo

```javascript
let str = 'My instructors shred';

// includes
console.log( str.includes('uct') );

// substr
console.log( str.substring(0,2) );

// charAt
console.log( str.charAt(0) );

// charCodeAt
console.log( str.charCodeAt(0) );
console.log( 'A'.charCodeAt(0) );

// fromCharCode
console.log( String.fromCharCode(77) );
```
